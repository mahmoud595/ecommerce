/**
 * Border radius values for the application theme
 */
import unit from '../unit';

const borderRadius = {
	none: 0,
	xs: 2 * unit,
	sm: 4 * unit,
	md: 6 * unit,
	lg: 8 * unit,
	xl: 12 * unit,
	'2xl': 16 * unit,
	'3xl': 24 * unit,
	full: 9999,
};

export default borderRadius;
